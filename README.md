# test-postgres

Test PostgreSQL with example transactions

## Setup

1. Install docker
1. Install docker-compose    
1. Install `psql`

    - MacOS:

        ```sh
        brew install libpq
        brew link --force libpq
        ```
